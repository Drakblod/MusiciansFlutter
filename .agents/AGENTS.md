# Custom Workspace Rules

- Whenever pushing changes to GitHub, always bump up the project version number (e.g., from 1.77 to 1.78) in both `pubspec.yaml` and the display strings in `lib/views/home_screen.dart`.
